# ============================================================
# AI Pencari Resep Masakan — requirements.txt
# ============================================================
# Catatan: Project ini sudah disesuaikan agar kompatibel dengan
# LangChain 1.x (perubahan struktur import besar dari versi 0.x).
# Semua import memakai path stabil dari `langchain_core`.
# ============================================================

# --- LangChain Core ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# --- LangChain Integrations ---
langchain-chroma>=0.1.4        # Chroma vectorstore adapter
langchain-ollama>=0.2.0        # Ollama chat & embedding

# --- LangGraph ---
langgraph>=0.2.0

# --- LangSmith (tracing & observability) ---
langsmith>=0.1.0

# --- Vector Database ---
chromadb>=0.5.0

# --- Web UI ---
streamlit>=1.38.0

# --- Utilities ---
python-dotenv>=1.0.0           # Baca file .env
typing-extensions>=4.9.0       # Untuk TypedDict & Literal di Python 3.10

# --- Optional: untuk pengembangan & testing ---
# pytest>=8.0.0
# black>=24.0.0
# ruff>=0.4.0
